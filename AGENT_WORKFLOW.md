# Deprecated Workflow Portal

The consolidated workflow now lives in [`AGENTS.md`](AGENTS.md). Update bookmarks and references accordingly.

This file remains temporarily to avoid broken external links. Remove any lingering dependencies on it and rely exclusively on the root handbook.

